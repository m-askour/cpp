#ifndef WHATEVER_HPP
#define WHATEVER_HPP

#include <iostream>
#include <string>
template<typename T> //this is for swap
    void swap(T &a, T &b);
template<typename T>//this is for min
    T min(T  &a, T  &b);
template<typename T>//this is for max 
    T max(T &a, T &b);
#endif